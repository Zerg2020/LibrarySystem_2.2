# 📤 Инструкция: Загрузка проекта в GitHub через Visual Studio

Эта инструкция поможет вам загрузить проект в GitHub прямо из Visual Studio.

## 🎯 Способ 1: Через встроенный Git в Visual Studio (Рекомендуется)

### Шаг 1: Инициализация Git репозитория

1. Откройте проект в Visual Studio
2. В правом нижнем углу найдите статус Git (или откройте **View** → **Git Changes**)
3. Если Git еще не инициализирован, нажмите **"Create Git Repository"**
4. Или в меню: **Git** → **Create Git Repository...**
5. В открывшемся окне:
   - Убедитесь, что путь указывает на вашу папку проекта
   - **НЕ** ставьте галочку "Add a README file" (он уже есть)
   - Нажмите **"Create"**

### Шаг 2: Добавление файлов в Git

1. Откройте окно **Git Changes** (View → Git Changes или Ctrl+0, G)
2. Вы увидите список измененных файлов
3. В поле **"Enter a commit message"** введите: `Initial commit with SonarCloud setup`
4. Нажмите кнопку **"Commit All"** (или **"Commit Staged"**)

### Шаг 3: Создание репозитория на GitHub

1. В окне **Git Changes** найдите раздел **"Push"** или кнопку **"Push to GitHub"**
2. Нажмите **"Push to GitHub"** или **"Publish to GitHub"**
3. Если вы не авторизованы:
   - Вас попросят войти в GitHub
   - Выберите **"Sign in with your browser"** или введите токен
4. В открывшемся окне:
   - **Repository name**: `LibrarySystem_1`
   - **Description**: `Library Management System on C++ and Qt` (опционально)
   - **Visibility**: 
     - Выберите **Public** (для бесплатного SonarCloud)
     - Или **Private** (если нужен приватный репозиторий)
   - **Add a README**: **НЕ** ставьте галочку (README уже есть)
   - **Add .gitignore**: **НЕ** ставьте галочку (.gitignore уже есть)
5. Нажмите **"Publish"** или **"Create"**

### Шаг 4: Проверка

1. После загрузки Visual Studio покажет ссылку на репозиторий
2. Откройте ссылку в браузере
3. Убедитесь, что все файлы загружены

---

## 🔄 Способ 2: Если репозиторий уже создан на GitHub

Если вы уже создали репозиторий на GitHub через веб-интерфейс:

### Шаг 1: Получите URL репозитория

1. Откройте ваш репозиторий на GitHub
2. Нажмите зеленую кнопку **"Code"**
3. Скопируйте URL (HTTPS или SSH)

### Шаг 2: Подключите remote в Visual Studio

1. В Visual Studio откройте **Git Changes**
2. Нажмите на стрелку рядом с кнопкой **"Push"**
3. Выберите **"View remotes"** или **"Manage Remotes"**
4. Нажмите **"Add"**
5. Введите:
   - **Name**: `origin`
   - **Fetch/Push**: вставьте URL вашего репозитория
6. Нажмите **"Save"**

### Шаг 3: Загрузите код

1. В окне **Git Changes** нажмите **"Push"**
2. Выберите remote `origin` и ветку `main` (или `master`)
3. Нажмите **"Push"**

---

## 🛠️ Способ 3: Через командную строку Git (Альтернатива)

Если встроенный Git в Visual Studio не работает:

1. Откройте **Terminal** в Visual Studio (View → Terminal)
2. Выполните команды:

```bash
# Инициализация (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit with SonarCloud setup"

# Добавление remote (замените URL на ваш)
git remote add origin https://github.com/ВАШ_USERNAME/LibrarySystem_1.git

# Переименование ветки в main (если нужно)
git branch -M main

# Загрузка в GitHub
git push -u origin main
```

---

## ⚠️ Важные замечания

### Перед загрузкой проверьте:

1. ✅ Файл `sonar-project.properties` обновлен с вашими ключами
2. ✅ Файл `.gitignore` настроен правильно
3. ✅ Не загружаются папки `build/`, `out/`, `.vs/`
4. ✅ Все исходные файлы на месте

### Что НЕ должно попасть в репозиторий:

- ❌ Папки `build/`, `out/`, `bin/`
- ❌ Папка `.vs/`
- ❌ Файлы `*.exe`, `*.dll`, `*.obj`
- ❌ `CMakeCache.txt` и другие временные файлы CMake
- ❌ Личные данные (если есть)

---

## 🔍 Проверка после загрузки

1. Откройте ваш репозиторий на GitHub
2. Проверьте структуру файлов:
   - ✅ Должны быть папки: `src/`, `include/`, `forms/`
   - ✅ Должны быть файлы: `CMakeLists.txt`, `README.md`, `sonar-project.properties`
   - ✅ Должна быть папка `.github/workflows/`
3. Перейдите в **Actions**:
   - Должны быть видны workflows: "Build and Test" и "SonarCloud Analysis"
   - После первого push они должны автоматически запуститься

---

## 🐛 Решение проблем

### Проблема: "Authentication failed"

**Решение:**
- В Visual Studio: **Tools** → **Options** → **Source Control** → **Git Global Settings**
- Проверьте настройки авторизации
- Или используйте Personal Access Token из GitHub

### Проблема: "Repository not found"

**Решение:**
- Убедитесь, что репозиторий создан на GitHub
- Проверьте правильность URL
- Убедитесь, что у вас есть права на запись в репозиторий

### Проблема: "Large files detected"

**Решение:**
- Проверьте `.gitignore` - возможно, большие файлы не исключены
- Удалите большие файлы из истории (если уже загружены)
- Проверьте папки `data/covers/` и `data/pdfs/` - возможно, там большие изображения

---

## 📝 После успешной загрузки

1. ✅ Проверьте, что workflow запустились в GitHub Actions
2. ✅ Настройте SonarCloud (см. `SONARCLOUD_SETUP.md`)
3. ✅ Добавьте `SONAR_TOKEN` в GitHub Secrets
4. ✅ Дождитесь первого анализа SonarCloud

---

**Готово!** Ваш проект теперь на GitHub и готов к анализу через SonarCloud! 🎉

