# 🚀 Быстрый старт для SonarCloud

## Минимальные шаги для запуска анализа

### 1️⃣ Создайте проект в SonarCloud (5 минут)

1. Зайдите на [sonarcloud.io](https://sonarcloud.io/) → Войдите через GitHub
2. Нажмите **"+"** → **"Analyze a new project"** → Выберите ваш репозиторий
3. Выберите **"With GitHub Actions"**
4. **Скопируйте** значения:
   - Organization key: `_____________`
   - Project key: `_____________`
   - Token: `_____________` (важно!)

### 2️⃣ Обновите sonar-project.properties (1 минута)

Откройте `sonar-project.properties` и замените:
```properties
sonar.projectKey=ВАШ_PROJECT_KEY
sonar.organization=ВАШ_ORGANIZATION_KEY
```

### 3️⃣ Добавьте токен в GitHub (2 минуты)

1. GitHub → Ваш репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret**:
   - Name: `SONAR_TOKEN_` (если `SONAR_TOKEN` занят, используйте `SONAR_TOKEN_`)
   - Value: ваш токен из шага 1
3. **Add secret**
   
**Примечание**: Workflow настроен на использование `SONAR_TOKEN_` (с подчеркиванием в конце).

### 4️⃣ Загрузите код в GitHub (если еще не загружен)

```bash
git add .
git commit -m "Add SonarCloud configuration"
git push
```

### 5️⃣ Проверьте результат

- GitHub → **Actions** → Должен запуститься workflow "SonarCloud Analysis"
- SonarCloud → Ваш проект → Увидите результаты анализа

---

📖 **Подробная инструкция**: См. файл [SONARCLOUD_SETUP.md](SONARCLOUD_SETUP.md)

