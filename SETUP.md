# Инструкция по настройке проекта для GitHub и SonarCloud

## Подготовка к загрузке в GitHub

### 1. Инициализация Git репозитория

```bash
git init
git add .
git commit -m "Initial commit"
```

### 2. Создание репозитория на GitHub

1. Создайте новый репозиторий на GitHub
2. Добавьте remote:
```bash
git remote add origin https://github.com/your-username/LibrarySystem_1.git
git branch -M main
git push -u origin main
```

## Настройка SonarCloud

**📖 Для подробной пошаговой инструкции см. [SONARCLOUD_SETUP.md](SONARCLOUD_SETUP.md)**

**🚀 Для быстрого старта см. [QUICK_START.md](QUICK_START.md)**

### Краткая версия:

1. Создайте проект на [SonarCloud](https://sonarcloud.io/) через GitHub
2. Обновите `sonar-project.properties` с вашими ключами
3. Добавьте `SONAR_TOKEN_` в GitHub Secrets (если `SONAR_TOKEN` занят, используйте `SONAR_TOKEN_`)
4. Загрузите код в GitHub - анализ запустится автоматически

## Структура файлов для CI/CD

- `.github/workflows/build.yml` - автоматическая сборка проекта
- `.github/workflows/sonarcloud.yml` - анализ кода через SonarCloud
- `sonar-project.properties` - конфигурация SonarCloud
- `.gitignore` - исключения для Git
- `.editorconfig` - настройки форматирования кода

## Проверка перед коммитом

Убедитесь, что:
- ✅ Все файлы сборки исключены в `.gitignore`
- ✅ `sonar-project.properties` содержит правильные ключи
- ✅ GitHub Actions workflows настроены правильно
- ✅ `SONAR_TOKEN_` добавлен в секреты GitHub (workflow настроен на использование `SONAR_TOKEN_`)

