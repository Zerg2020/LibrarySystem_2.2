# 📋 Подробная инструкция по настройке SonarCloud

Эта инструкция поможет вам настроить SonarCloud для автоматического анализа кода вашего проекта.

## 🎯 Шаг 1: Регистрация на SonarCloud

1. Перейдите на сайт [SonarCloud.io](https://sonarcloud.io/)
2. Нажмите кнопку **"Log in"** (Войти)
3. Выберите **"Log in with GitHub"** и авторизуйтесь через ваш GitHub аккаунт
4. При первом входе SonarCloud попросит разрешения на доступ к вашим репозиториям - нажмите **"Authorize"**

## 🏢 Шаг 2: Создание организации (если еще нет)

1. После входа вы попадете на главную страницу SonarCloud
2. Если у вас еще нет организации, нажмите **"Create Organization"**
3. Выберите план:
   - **Free Plan** - бесплатный план для публичных репозиториев (рекомендуется для начала)
   - **Paid Plan** - платный план для приватных репозиториев
4. Заполните информацию об организации:
   - **Organization key** - это будет ваш уникальный ключ (например: `my-org-key`)
   - **Display name** - отображаемое имя
   - **URL** - опционально
5. Нажмите **"Create Organization"**

## 📦 Шаг 3: Создание проекта в SonarCloud

1. На главной странице SonarCloud нажмите **"+"** или **"Add Project"**
2. Выберите **"Analyze a new project"**
3. Выберите **"From GitHub"**
4. Найдите ваш репозиторий `LibrarySystem_1` в списке
5. Нажмите **"Set Up"** рядом с вашим репозиторием
6. SonarCloud предложит несколько вариантов настройки:
   - Выберите **"With GitHub Actions"** (рекомендуется)
7. На следующем экране вы увидите:
   - **Organization key** (например: `my-org-key`)
   - **Project key** (например: `my-org-key_LibrarySystem_1`)
   - **Token** - это важно! Скопируйте его или запомните, где его найти

## 🔑 Шаг 4: Получение токена SonarCloud

Если вы не скопировали токен на предыдущем шаге:

1. В SonarCloud перейдите в **"My Account"** (правый верхний угол)
2. Выберите **"Security"** в меню
3. В разделе **"Generate Tokens"**:
   - **Token name**: введите имя, например `LibrarySystem_1`
   - Нажмите **"Generate"**
4. **ВАЖНО**: Скопируйте токен сразу! Он показывается только один раз
5. Сохраните токен в безопасном месте (например, в менеджере паролей)

## ⚙️ Шаг 5: Обновление конфигурации проекта

1. Откройте файл `sonar-project.properties` в корне проекта
2. Найдите строки:
   ```properties
   sonar.projectKey=your-organization-key_LibrarySystem_1
   sonar.organization=your-organization-key
   ```
3. Замените `your-organization-key` на ваш реальный ключ организации из SonarCloud
4. Пример:
   ```properties
   sonar.projectKey=my-org-key_LibrarySystem_1
   sonar.organization=my-org-key
   ```
5. Сохраните файл

## 🔐 Шаг 6: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (Настройки) репозитория
3. В левом меню выберите **"Secrets and variables"** → **"Actions"**
4. Нажмите **"New repository secret"**
5. Заполните форму:
   - **Name**: `SONAR_TOKEN_` (если `SONAR_TOKEN` занят, используйте `SONAR_TOKEN_` с подчеркиванием)
   - **Secret**: вставьте токен, который вы получили в SonarCloud
   
   **Примечание**: Workflow настроен на использование `SONAR_TOKEN_` (с подчеркиванием в конце). Если вы используете другое имя, обновите `.github/workflows/sonarcloud.yml`.
6. Нажмите **"Add secret"**

## 📤 Шаг 7: Загрузка проекта в GitHub

Если проект еще не загружен в GitHub:

1. Инициализируйте Git репозиторий (если еще не сделано):
   ```bash
   git init
   ```

2. Добавьте все файлы:
   ```bash
   git add .
   ```

3. Создайте первый коммит:
   ```bash
   git commit -m "Initial commit with SonarCloud setup"
   ```

4. Создайте репозиторий на GitHub:
   - Перейдите на [github.com](https://github.com)
   - Нажмите **"+"** → **"New repository"**
   - Название: `LibrarySystem_1`
   - Выберите **Public** (для бесплатного SonarCloud) или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
   - Нажмите **"Create repository"**

5. Подключите локальный репозиторий к GitHub:
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/LibrarySystem_1.git
   git branch -M main
   git push -u origin main
   ```

## ✅ Шаг 8: Проверка работы

1. После загрузки кода в GitHub, перейдите в раздел **"Actions"** вашего репозитория
2. Вы должны увидеть workflow **"SonarCloud Analysis"** в списке
3. Если workflow запустился автоматически - отлично! Если нет:
   - Перейдите в **"Actions"** → **"SonarCloud Analysis"**
   - Нажмите **"Run workflow"** → **"Run workflow"**

4. Дождитесь завершения workflow (обычно 2-5 минут)
5. Проверьте результаты:
   - В GitHub: перейдите в **"Actions"** и откройте последний запуск
   - В SonarCloud: перейдите на главную страницу вашего проекта

## 🔍 Шаг 9: Просмотр результатов анализа

1. Откройте ваш проект в SonarCloud
2. Вы увидите:
   - **Overview** - общая информация о качестве кода
   - **Issues** - найденные проблемы
   - **Measures** - метрики кода
   - **Code** - просмотр кода с выделением проблем

3. В GitHub:
   - Результаты анализа будут видны в разделе **"Pull Requests"** как проверки (checks)
   - При создании PR вы увидите статус проверки SonarCloud

## 🛠️ Устранение проблем

### Проблема: Workflow не запускается

**Решение:**
- Проверьте, что файл `.github/workflows/sonarcloud.yml` существует
- Убедитесь, что вы загрузили код в ветку `main`, `master` или `develop`
- Проверьте, что `SONAR_TOKEN_` правильно добавлен в Secrets

### Проблема: "Invalid project key"

**Решение:**
- Проверьте файл `sonar-project.properties`
- Убедитесь, что `sonar.projectKey` и `sonar.organization` соответствуют значениям из SonarCloud
- Формат должен быть: `organization-key_project-name`

### Проблема: "Authentication failed"

**Решение:**
- Проверьте, что `SONAR_TOKEN_` правильно добавлен в GitHub Secrets
- Убедитесь, что токен не истек (токены не имеют срока действия, но можно создать новый)
- Проверьте, что имя секрета точно `SONAR_TOKEN_` (с подчеркиванием в конце, чувствительно к регистру)

### Проблема: "No files to analyze"

**Решение:**
- Проверьте, что в `sonar-project.properties` правильно указаны пути:
  ```properties
  sonar.sources=src,include,forms
  ```
- Убедитесь, что файлы не исключены в `sonar.exclusions`

### Проблема: "The only way to get an accurate analysis... sonar.cfamily.compile-commands"

**Симптомы:**
```
ERROR Error during SonarScanner Engine execution
java.lang.UnsupportedOperationException: 
The only way to get an accurate analysis of C/C++/Objective-C files 
in Manual Configuration mode is to provide a compilation database 
through the property "sonar.cfamily.compile-commands"
```

**Решение:**
1. Убедитесь, что в `sonar-project.properties` указан правильный путь:
   ```properties
   sonar.cfamily.compile-commands=build/compile_commands.json
   ```
   ⚠️ **НЕ используйте** `sonar.cfamily.compile-commands.build-dir=build` - это устаревший формат!

2. Убедитесь, что `CMAKE_EXPORT_COMPILE_COMMANDS=ON` в `CMakeLists.txt`

3. После исправления закоммитьте и запушьте изменения:
   ```bash
   git add sonar-project.properties
   git commit -m "Fix: Correct sonar.cfamily.compile-commands path"
   git push
   ```

## 📊 Что анализирует SonarCloud

SonarCloud проверяет:
- ✅ **Bugs** - ошибки в коде
- ✅ **Vulnerabilities** - уязвимости безопасности
- ✅ **Code Smells** - проблемы с качеством кода
- ✅ **Coverage** - покрытие тестами (если настроено)
- ✅ **Duplications** - дублирование кода
- ✅ **Complexity** - сложность кода
- ✅ **Maintainability** - поддерживаемость

## 🎓 Полезные ссылки

- [Документация SonarCloud](https://docs.sonarcloud.io/)
- [Настройка C++ проектов](https://docs.sonarcloud.io/languages/cpp/)
- [GitHub Actions для SonarCloud](https://docs.sonarcloud.io/integrations/ci/github-actions/)

## ✨ Готово!

После выполнения всех шагов ваш проект будет автоматически анализироваться SonarCloud при каждом push и pull request. Результаты будут видны как в SonarCloud, так и в GitHub.

---

**Примечание**: Если у вас возникли проблемы, проверьте логи в GitHub Actions или обратитесь к документации SonarCloud.

